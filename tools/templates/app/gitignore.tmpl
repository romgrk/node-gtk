# dependencies
node_modules/

# build output
dist/

# TypeScript types generated per-machine by `node-gtk generate-types`
# (regenerated on install — never commit).
.node-gtk-types/

# misc
*.log
.DS_Store
