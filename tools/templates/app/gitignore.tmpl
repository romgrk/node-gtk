# dependencies
node_modules/

# build output
dist/

# TypeScript types generated per-machine by `node-gtk generate-types`
# (lives under node_modules by default — regenerate, never commit).
.node-gtk-types/

# misc
*.log
.DS_Store
