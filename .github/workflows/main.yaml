name: main
on: [push]
jobs:
  build:
    name: ${{ matrix.os }}-${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-18.04
            node: 15
          - os: ubuntu-18.04
            node: 14
    steps:
      - uses: actions/checkout@v2

      # Install dependencies
      - if: matrix.os == 'ubuntu-18.04'
        run: ./scripts/github-actions/install-deps-ubuntu.sh
      - if: matrix.os == 'macos-latest'
        run: ./scripts/github-actions/install-deps-macos.sh

      # Install Nodejs version
      - run: nvm install ${{ matrix.node }}
      - run: nvm use ${{ matrix.node }}
      - run: node --version
      - run: npm --version

      - run: export COMMIT_MESSAGE=$(git show -s --format=%B $TRAVIS_COMMIT | tr -d '\n')

      - run: ./scripts/build.sh
      # - uses: romgrk/publish-to-github@master
        # env:
          # GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
